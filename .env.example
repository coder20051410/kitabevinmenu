# Copy to .env.local and fill these values from Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=
# Only use ADMIN_PASSWORD for local fallback when Supabase Auth is not configured.
BLOB_READ_WRITE_TOKEN=
